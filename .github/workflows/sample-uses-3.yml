name: Sample (uses-3)
on:
  push:

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - uses: tmm1/gha-repro-self-referential/.github/workflows/bootstrap/action.yml@main
        with:
          msg: foo